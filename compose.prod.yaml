services:
  caddy:
    restart: always
    ports:
      - "${CADDY_HTTP_PORT:-80}:80"
      - "${CADDY_HTTPS_PORT:-443}:443"

  api:
    restart: always

  xray:
    restart: always
    ports:
      - "${XRAY_PORT:-8443}:443/tcp"

  wireguard:
    restart: always
